export default function QuizPage() {
  return (
    <main className="relative flex min-h-screen items-center justify-center bg-cream px-6 text-ink">
      <div className="text-center">
        <span className="text-xs uppercase tracking-[0.2em] text-ink-muted">
          Coming in Phase 4
        </span>
        <h1 className="mt-4 text-display-md font-semibold">
          Quiz flow lands here
        </h1>
        <p className="mt-3 text-ink-muted">
          Profession select → 10 questions → email → results → paywall.
        </p>
      </div>
    </main>
  );
}
